# コマンド関連 @Ubuntu18.04

```bash
$ sudo service apache2 [restart|start]
$ sudo systemctl [start|restart|reload|
$ sudo apache2ctl [graceful|start|restart]

$ sudo a2ensite hogesite.conf
$ sudo dissite hogesite.conf
$ sudo a2enmod ssl
$ sudo a2dismod ssl
```